#include <task2.h>
#include <iostream>
using namespace std;

int main() {
 	unsigned int example = 20;
 	cout << checkPrime(example) << endl;
 	cout << nPrime(example) << endl;
 	cout << nextPrime(example) << endl;
}
